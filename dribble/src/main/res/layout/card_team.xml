<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <data class=".databinding.TeamItemBinding">

        <import type="android.text.Html"/>

        <variable
            name="team"
            type="com.thea.fordesign.bean.DribbbleTeam"/>

        <variable
            name="actionHandler"
            type="com.thea.fordesign.team.TeamItemPresenter"/>
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{(v) -> actionHandler.openTeamDetails(team, v)}">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/normal_vertical_padding"
            android:paddingBottom="@dimen/normal_vertical_padding"
            android:paddingLeft="@dimen/normal_horizontal_margin"
            android:paddingRight="@dimen/normal_horizontal_margin">

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/iv_avatar"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:scaleType="centerCrop"
                android:transitionName="@string/image_user_avatar"
                tools:ignore="ContentDescription"
                tools:src="@mipmap/default_user_avatar"/>

            <TextView
                android:id="@+id/tv_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignTop="@id/iv_avatar"
                android:layout_marginLeft="@dimen/normal_horizontal_margin"
                android:layout_toEndOf="@id/iv_avatar"
                android:layout_toRightOf="@id/iv_avatar"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:lines="1"
                android:text="@{team.name}"
                android:textColor="@color/dribbble_link_blue"
                tools:text="@string/default_designer"/>

            <TextView
                android:id="@+id/tv_bio"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignBottom="@id/iv_avatar"
                android:layout_below="@id/tv_name"
                android:layout_alignLeft="@id/tv_name"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:lines="1"
                android:text="@{Html.fromHtml(team.bio ?? &quot;&quot;)}"
                android:textColor="@color/grey_400"
                android:textSize="@dimen/text_size_caption"
                tools:text="@string/default_time"/>
        </RelativeLayout>
    </android.support.v7.widget.CardView>
</layout>